<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="25dp" />
    <solid android:color="@color/enter_field"/>
    <stroke
        android:width="2dp"
        android:color="@color/ef_stroke" />
</shape>